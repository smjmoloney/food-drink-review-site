{% extends 'base.html.twig' %}
{% block body %}
    {% if error %}
        <div>{{ error.messageKey|trans(error.messageData, 'security') }}</div>
    {% endif %}
    <h3>Sign Up</h3>
    {% include 'security/_form.html.twig' with {'form': form} only %}
{% endblock %}