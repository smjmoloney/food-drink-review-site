{{ form_start(form, { 'attr' : { 'class': 'form-horizontal' } }) }}
    {{ form_widget(form) }}
    <button class="btn btn-default mr-sm-2">{{ button_label|default('Save') }}</button>
{{ form_end(form) }}